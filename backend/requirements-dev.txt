# Adaptly backend — development/test-only dependencies
# Install:  pip install -r requirements.txt -r requirements-dev.txt
#
# Not needed to run the app — only to run the test suite.

pytest>=9.1.1
mongomock>=4.3.0
